import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Manual Match Details",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
